# Phase 3C QA Checklist

- Build the app successfully with `npm run build`
- Verify `/dashboard` has one KPI row, one action row, and clearer main workspace focus
- Verify `/approvals` shows simplified summary + priority strip and cleaner queue rows
- Verify `/projects` feels less top-heavy and the table/workspace dominates
- Verify `/analytics` reads as interpretation, not a second dashboard
- Verify `/admin/activity` is lighter and easier to scan
- Verify `/admin/notifications` does not feel duplicative of Dashboard
- Verify `/admin/exports` and `/admin/billing` remain stable and calmer
- Verify admin / manager / contractor role access is unchanged
